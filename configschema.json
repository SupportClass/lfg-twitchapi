{
	"$schema": "http://json-schema.org/draft-04/schema#",
	"type": "object",
	"properties": {
		"username": {
			"type": "string",
			"description": "The Twitch account that API requests should be sent as."
		},
		"required": [
			"username"
		]
	}
}
